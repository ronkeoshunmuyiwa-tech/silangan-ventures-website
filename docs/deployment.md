# Deployment (Reference)

This project is Vercel-compatible but is **not yet deployed**.

Planned steps for a future session:
1. Create a Vercel account/project linked to this GitHub repository.
2. Connect the `main` branch for production deployments.
3. Add environment variables in Vercel (none required yet — none exist in this project).
4. Confirm the production build succeeds on Vercel.
5. Connect the custom domain once purchased.

Do not deploy until Paula has approved the relevant step.
